<form method="post" action="{{ path('app_product_service_delete', {'id': product_service.id}) }}" onsubmit="return confirm('¿Estás segura de que querés eliminar este registro?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_service.id) }}">
    <button class="btn btn-outline-danger">
        <i class="bi bi-trash"></i> Eliminar
    </button>
</form>
