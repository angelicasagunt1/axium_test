{% extends 'base.html.twig' %}

{% block title %}Nuevo Producto o Servicio{% endblock %}

{% block body %}
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
            <h5 class="mb-0">Crear nuevo Producto o Servicio</h5>
        </div>
        <div class="card-body">
            {{ include('product_service/_form.html.twig') }}
        </div>
    </div>

    <div class="mt-3">
        <a href="{{ path('app_product_service_index') }}" class="btn btn-secondary">
            ← Volver al listado
        </a>
    </div>
{% endblock %}
